<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ErrorMassage">
        <item name="android:textColor">#FF0000</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="backgroundButtons">
        <item name="android:background">@drawable/buttons_background</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="rfn009Text">
        <item name="android:background">@drawable/ic_text_background</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">semi-bold</item>
    </style>

</resources>